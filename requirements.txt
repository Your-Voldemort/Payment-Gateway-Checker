aiogram>=3.24.0
requests==2.31.0
aiohttp==3.13.4
aiodns==3.1.1
python-dotenv==1.2.2
validators==0.22.0

# Database
aiosqlite>=0.19.0

# HTML parsing for structured element analysis
beautifulsoup4>=4.12.0
lxml>=5.0.0

# TLS fingerprint bypass for CDN/WAF that use JA3 fingerprinting (Fastly, Cloudflare, Akamai)
# This allows the bot to access sites that block Python's default TLS fingerprint
curl_cffi>=0.6.0

# Optional performance optimization (10-20x faster pattern matching)
# Install separately: pip install pyahocorasick
pyahocorasick>=2.0.0

# Fake user agent for rotating realistic user agents
fake-useragent>=2.2.0